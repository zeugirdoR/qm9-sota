# Notebooks

Notebook rule:

- Notebooks are launchers only.
- Do not define models, losses, splits, or training loops in notebook cells.
- All real code belongs in `src/`, `scripts/`, and `configs/`.

Use the launcher cells in the repository README to clone, install, and run scripts from Colab.
